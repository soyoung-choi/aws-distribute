{
  "name": "aws-distribute",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "client-build": "cd frontend && npm ci && npm run build && cd ../",
    "prebuild": "npm run client-build && mv ./frontend/build ./backend/dist",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "prettier": "^2.7.1"
  }
}
